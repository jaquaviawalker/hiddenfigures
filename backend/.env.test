# Test environment settings
# These are the PostgreSQL credentials for testing
# Make sure you have this database created before running integration tests
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hiddenfigures_test
JWT_SECRET=test_secret_key_123456789
